#include <iostream>
#include <iomanip>
#include <string.h>
using namespace std;

int main() {
    int num_a, num_b, sum_a_b;
    cin >> num_a;
    cin >> num_b;
    sum_a_b = num_a + num_b;
    cout << "SOMA = " << sum_a_b << "\n";
    return 0;
}
